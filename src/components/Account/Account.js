import React from 'react';

const Account = () => {
	return(
		<nav style={{display: 'flex', justifyContent: 'flex-end'}}>
			<p className='f3 pointer link'>Account</p>
		</nav>
	);
}

export default Account;